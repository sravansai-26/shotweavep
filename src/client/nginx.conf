server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Handle React routing (history mode)
    location / {
        try_files $uri $uri/ /index.html;
    }
}